//OptionalChaiing trong typeScript là một cú pháp cho phép truy cập đến các thuộc tính hoặc một object mà
// không cần object đó có tồn tại hay không khi truy cập các thuộc tính  bên trong đối tượng (khi mà truy
// cập mot thuoc tính undefine cua một đối tuong nó sẽ báo lỗi )=> thêm optional ( là dấu ? ) để tránh lỗi
